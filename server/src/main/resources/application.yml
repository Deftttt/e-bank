spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ebank_database
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

security:
  jwt:
    secret-key: alamakotakotmaaleblablabla

logging:
  level:
    org.springframework.security: DEBUG

